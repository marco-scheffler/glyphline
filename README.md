# Glyphline

Glyphline is a native macOS SwiftUI app scaffolded with XcodeGen.

## Generate the project

```bash
xcodegen generate
```

## Build

```bash
xcodebuild -project Glyphline.xcodeproj -scheme Glyphline -destination 'platform=macOS' build
```
